// Autogenerated from development/constructs/block_construct.i

package ideal.development.constructs;

import ideal.library.elements.*;
import ideal.runtime.elements.*;
import ideal.development.elements.*;
import ideal.development.names.*;

public class block_construct extends base_construct {
  public final readonly_list<annotation_construct> annotations;
  public final readonly_list<construct> body;
  public block_construct(final readonly_list<annotation_construct> annotations, final readonly_list<construct> body, final origin the_origin) {
    super(the_origin);
    this.annotations = annotations;
    this.body = body;
  }
  public block_construct(final readonly_list<construct> body, final origin the_origin) {
    this(new empty<annotation_construct>(), body, the_origin);
  }
  public @Override readonly_list<construct> children() {
    final base_list<construct> generated_result = new base_list<construct>();
    generated_result.append_all((readonly_list<construct>) (readonly_list) this.annotations);
    generated_result.append_all(this.body);
    return generated_result;
  }
}
