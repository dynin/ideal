// Autogenerated from development/constructs/constraint_category.i

package ideal.development.constructs;

import ideal.library.elements.*;
import ideal.runtime.elements.*;
import ideal.development.elements.*;
import ideal.development.names.*;

public enum constraint_category implements deeply_immutable_data, stringable {
  ASSERT_CONSTRAINT(new base_string("assert")),
  VERIFY_CONSTRAINT(new base_string("verify"));
  private final string name_string;
  private constraint_category(final string name_string) {
    this.name_string = name_string;
  }
  public simple_name constraint_name() {
    return simple_name.make(this.name_string);
  }
  public @Override string to_string() {
    return this.name_string;
  }
}
