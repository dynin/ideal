// Autogenerated from development/constructs/grammar_construct.i

package ideal.development.constructs;

import ideal.library.elements.*;
import ideal.runtime.elements.*;
import ideal.development.elements.*;
import ideal.development.names.*;

public class grammar_construct extends base_construct {
  public final readonly_list<annotation_construct> annotations;
  public final action_name name;
  public final readonly_list<construct> body;
  public @Override string to_string() {
    return utilities.describe(this, this.name);
  }
  public grammar_construct(final readonly_list<annotation_construct> annotations, final action_name name, final readonly_list<construct> body, final origin generated_origin) {
    super(generated_origin);
    assert annotations != null;
    this.annotations = annotations;
    assert name != null;
    this.name = name;
    assert body != null;
    this.body = body;
  }
  public @Override readonly_list<construct> children() {
    final base_list<construct> generated_result = new base_list<construct>();
    generated_result.append_all((readonly_list<construct>) (Object) this.annotations);
    generated_result.append_all(this.body);
    return generated_result;
  }
}
