// Autogenerated from library/patterns.i

package ideal.library.patterns;

import ideal.library.elements.*;

import javax.annotation.Nullable;

public interface reversible_pattern<element_type> extends pattern<element_type> {
  @Nullable range find_last(readonly_list<element_type> the_list, @Nullable Integer end_index);
}
