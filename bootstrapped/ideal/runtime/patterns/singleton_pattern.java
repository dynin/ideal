// Autogenerated from runtime/patterns/singleton_pattern.i

package ideal.runtime.patterns;

import ideal.library.elements.*;
import ideal.library.patterns.*;
import ideal.runtime.elements.*;

public class singleton_pattern<element_type> extends one_pattern<element_type> {
  public final element_type singleton_element;
  public singleton_pattern(final element_type singleton_element) {
    this.singleton_element = singleton_element;
  }
  public @Override boolean matches(final element_type the_element) {
    return ideal.machine.elements.runtime_util.values_equal(the_element, this.singleton_element);
  }
}
